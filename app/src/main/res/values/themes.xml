<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TrasnparentStatusbar" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- set to transparent if your app is drawing behind the status bar. -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!-- set the status bar light and content dark. -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.TrasnparentStatusbar" parent="Base.Theme.TrasnparentStatusbar" />
</resources>